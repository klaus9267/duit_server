name: CD - Deploy

on:
  pull_request:
    branches: [main]
    types: [closed]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-deploy:
    if: |
      github.event_name == 'workflow_dispatch' || 
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,prefix={{branch}}-

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Create build configuration
        run: |
          # Docker 빌드용 설정 파일 생성
          cat > src/main/resources/application.properties << EOF
          ${{ secrets.PROPERTIES }}
          EOF

          cat > src/main/resources/firebase-key.json << EOF
          ${{ secrets.FIREBASE_KEY }}
          EOF

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy to production server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.DEPLOY_PORT }}
          script: |
            echo "🚀 배포 시작..."
            cd /home/vagom/duit-server
  
            # Docker 레지스트리 로그인
            echo "::notice::Docker 레지스트리 로그인 중..."
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            
            # 최신 이미지 다운로드
            echo "::notice::최신 이미지 다운로드 중..."
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            
            # 현재 활성 환경 감지
            echo "::group::🔍 현재 활성 환경 감지"
            CURRENT_ENV="blue"  # 기본값
            
            # Blue 환경 체크
            if docker ps --format "{{.Names}}" | grep -q "duit-server-blue"; then
              if curl -f -s http://localhost:8080/actuator/health >/dev/null 2>&1; then
                CURRENT_ENV="blue"
              fi
            fi
            
            # Green 환경 체크  
            if docker ps --format "{{.Names}}" | grep -q "duit-server-green"; then
              if curl -f -s http://localhost:8081/actuator/health >/dev/null 2>&1; then
                CURRENT_ENV="green"
              fi
            fi
            
            # 타겟 환경 결정
            if [ "$CURRENT_ENV" = "blue" ]; then
              TARGET_ENV="green"
              TARGET_PORT="8081"
            else
              TARGET_ENV="blue"
              TARGET_PORT="8080"
            fi
            
            echo "::notice::Current active environment: $CURRENT_ENV"
            echo "::notice::Target deployment environment: $TARGET_ENV"
            echo "::notice::Target port: $TARGET_PORT"
            echo "::endgroup::"
            
            # 타겟 환경에 배포
            echo "::group::🚀 Deploying to $TARGET_ENV Environment"
            echo "::notice::Setting environment variables for $TARGET_ENV..."
            
            # 환경별 버전 설정
            if [ "$TARGET_ENV" = "blue" ]; then
              export BLUE_VERSION="latest"
              export GREEN_VERSION="${GREEN_VERSION:-latest}"
            else
              export GREEN_VERSION="latest"  
              export BLUE_VERSION="${BLUE_VERSION:-latest}"
            fi
            
            echo "::notice::Starting $TARGET_ENV environment..."
            docker-compose --profile $TARGET_ENV up -d app-$TARGET_ENV
            echo "::notice title=Success::✅ $TARGET_ENV environment started"
            echo "::endgroup::"
            
            # 헬스체크 (기존 로직을 Blue-Green에 맞게 수정)
            echo "::group::🏥 Health Check - $TARGET_ENV Environment"
            echo "::notice::Target: http://localhost:$TARGET_PORT"
            
            HEALTH_SUCCESS=false
            for i in {1..60}; do
              # 컨테이너가 실행 중인지 확인
              if ! docker ps --format "{{.Names}}" | grep -q "duit-server-$TARGET_ENV"; then
                echo "::error::Container duit-server-$TARGET_ENV is not running"
                break
              fi
              
              # 헬스체크 실행
              if docker exec duit-server-$TARGET_ENV curl -f -s http://localhost:8080/actuator/health >/dev/null 2>&1; then
                echo "::notice title=Success::✅ $TARGET_ENV environment is healthy! (attempt $i)"
                HEALTH_SUCCESS=true
                break
              fi
              
              if [ $i -eq 60 ]; then
                echo "::error title=Health Check Failed::❌ Health check timeout after 60 attempts (3 minutes)"
                echo "::group::📋 Container Logs"
                docker logs --tail 50 duit-server-$TARGET_ENV || true
                echo "::endgroup::"
                break
              else
                echo "::notice::Health check attempt $i/60 - waiting 3s..."
                sleep 3
              fi
            done
            echo "::endgroup::"
            
            # 헬스체크 결과에 따른 처리
            if [ "$HEALTH_SUCCESS" = true ]; then
              # 트래픽 전환
              echo "::group::🔄 Traffic Switching"
              echo "::notice::Switching traffic to $TARGET_ENV environment..."
              
              if [ -f "scripts/switch-traffic.sh" ]; then
                chmod +x scripts/switch-traffic.sh
                ./scripts/switch-traffic.sh $TARGET_ENV
                echo "::notice title=Success::✅ Traffic switched to $TARGET_ENV"
              else
                echo "::warning::Traffic switching script not found. Manual configuration required."
                echo "::notice::Please update Nginx configuration to route traffic to $TARGET_ENV (port $TARGET_PORT)"
              fi
              echo "::endgroup::"
              
            else
              # 배포 실패 시 롤백
              echo "::group::🔄 Rollback Process"  
              echo "::error::Health check failed. Rolling back $TARGET_ENV environment..."
              docker-compose --profile $TARGET_ENV stop app-$TARGET_ENV || true
              docker-compose --profile $TARGET_ENV rm -f app-$TARGET_ENV || true
              echo "::notice::Rollback completed. $CURRENT_ENV environment remains active."
              echo "::endgroup::"
              echo "::endgroup::"  # Close main deployment group
              exit 1
            fi
            echo "::endgroup::"

      - name: Post-deployment verification
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.DEPLOY_PORT }}
          script: |
            echo "::group::🔍 Deployment Verification"
            cd /home/vagom/duit-server
            
            # 최종 헬스체크 (외부에서 접근)
            echo "::notice::Final health check via external endpoint..."
            sleep 10  # 트래픽 전환 후 안정화 대기
            
            if curl -f -s ${{ secrets.BASE_URL }}/actuator/health >/dev/null 2>&1; then
              echo "::notice title=Success::✅ External health check passed"
            else
              echo "::warning::External health check failed, but deployment may still be successful"
            fi
            
            # 배포 상태 확인
            echo "::group::📊 Deployment Status"
            echo "::notice::Container status:"
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}" | grep duit || echo "No duit containers found"
            
            # 이전 이미지 정리
            echo "::notice::Cleaning up old images..."
            docker image prune -f
            
            echo "::notice title=Success::🎉 Blue-Green deployment completed successfully!"
            echo "::endgroup::"

      - name: Create deployment summary
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "### 🎉 Blue-Green 배포 완료!" >> $GITHUB_STEP_SUMMARY
            echo "**배포 시간:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
            echo "**이미지:** ${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
            echo "**배포 방식:** Blue-Green 무중단 배포" >> $GITHUB_STEP_SUMMARY
            echo "**헬스체크:** /actuator/health" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "✅ 무중단 배포가 성공적으로 완료되었습니다!" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ❌ 배포 실패" >> $GITHUB_STEP_SUMMARY
            echo "**실패 시간:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
            echo "**오류:** 배포 과정에서 문제가 발생했습니다." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "기존 환경이 유지되고 있으니 서비스에는 영향이 없습니다." >> $GITHUB_STEP_SUMMARY
          fi